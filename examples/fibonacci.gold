let fib = {
    let recurse = (f, n, val) => {
        if n == 0 then
            val
        else
            f(f, n-1, [val[1], val[0] + val[1]])
    }
    in (n) => recurse(recurse, n, [1, 1])[1]
}

in fib(5)
